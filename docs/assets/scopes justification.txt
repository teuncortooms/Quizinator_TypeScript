The addon uses ../auth/spreadsheets to read idioms data from a spreadsheet and creates an example sheet, and uses ../auth/documents and ../auth/forms to make and fill documents and forms with quizzes, based on the data and user input. 